# CHECK 6: Trade Secret Exception (Article 8.6)
# Verify if data refusal is justified by trade secret
# Returns: true/false (ASK query)

PREFIX dataact: <http://www.semanticweb.org/dataact#>

ASK {
  # Data contains trade secret
  ?data dataact:containsTradeSecret ?secret .
  ?secret a dataact:TradeSecret .
  
  # Rule permits refusal
  dataact:article8_6_rule dataact:permits ?permission .
  ?permission dataact:specifiesAction dataact:refuseDataAccessAction .
}